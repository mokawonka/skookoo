var epubid = "<%= @epubid %>";

$('#submitepub').hide();
$('#createfromdb').hide();

if (epubid && epubid !== "") {
    window.epubid = epubid;
    $('#createfromdb').css('display', 'inline-block');
} else {
    $('#submitepub').css('display', 'inline-block');
}